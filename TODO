* Gate re-trigger mode
  When this mode is selected, gate should always re-trigger on note on.

* MIDI boot loader
  It should be possible to update firmware via MIDI.

* Mode that mimics the eurorack module Turing Machine by Music Thing Modular
  Seems very rational for a midi to cv circuit.
